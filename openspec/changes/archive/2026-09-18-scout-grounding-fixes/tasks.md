## 1. Name folding and resolution

- [x] 1.1 `lib/scout/context.ts`: export pure `foldName(s)` — NFD, strip `\p{M}`, fold table (`ø→o æ→ae ß→ss œ→oe ð→d þ→th ł→l đ→d`, upper-case variants), lowercase, non-alphanumerics → single space, trim.
- [x] 1.2 `resolvePlayer`: fold the query; match folded `webName` and folded `fullName` with precedence exact-web → exact-full → prefix (either) → substring (either); ties by `totalPoints`. Keep numeric-id resolution.
- [x] 1.3 Export `suggestPlayers(query, sc, limit = 5)`: folded prefix/substring near-misses across both names, highest `totalPoints` first.
- [x] 1.4 Tests (`lib/__tests__/scout/context.test.ts` or new `name-resolution.test.ts`): `odegaard`/`gross`/`joao pedro`/`sangare`/`martin odegaard` resolve; `saka` unchanged; precedence (exact beats substring); suggestions for a typo.

## 2. Honest misses

- [x] 2.1 `lib/scout/tools.ts`: `score_player`, `compare_players` (per name), `simulate_transfer` (per side), `simulate_captain` return `{ notFound: true, query, suggestions }` on a miss; no `error` string for a plain miss.
- [x] 2.2 `lib/scout/system-prompt.ts`: Grounding rule (a) — not-found means the name did not match; report it, list suggestions, ask for the spelling; never infer injury/absence/transfer/unavailability. Add to both manager and demo prompts.
- [x] 2.3 Tests in `lib/__tests__/scout/tools.test.ts`: not-found payload shape for each tool; `compare_players` with one miss keeps the other row. `system-prompt-knowledge.test.ts`: rule text present in both prompts.

## 3. Squad awareness

- [x] 3.1 `ScoutContext` gains `ownedById: Map<number, "xi" | "bench">` built from `ctx.analysis.picks` in `buildScoutContext`; `fmtPlayer(sp, sc)` adds `owned`.
- [x] 3.2 `search_players`: `excludeOwned` input (default true) filters `ownedById` out of the pool before shortlisting; description rewritten to "Find TRANSFER TARGETS …; excludes the manager's own players unless excludeOwned is false".
- [x] 3.3 `lib/scout/system-prompt.ts`: Grounding rule (b) — a transfer target is never an owned player; owned-player questions are lineup calls; never count an owned player's price against a transfer budget. Both prompts.
- [x] 3.4 Tests: `owned` tagging (xi/bench/null) on `score_player` and `search_players`; default search returns no owned rows; `excludeOwned: false` includes them; rule text present.

## 4. Verify

- [x] 4.1 `tsc`/`eslint`/`vitest` green; research harness typechecks; both replays byte-identical.
- [x] 4.2 *(verified live 2026-09-18 20:1xZ after credit was restored — see as-built)* Manual chat check against the live squad (dev server, three prompts): "score odegaard" resolves; "score Odegard" (typo) yields a not-found answer with suggestions and no invented reason; "which forward under £7.7m should replace Wissa" lists no owned player and, if it mentions João Pedro at all, frames him as a lineup call. Record the three answers in the as-built note.
- [x] 4.3 As-built note; archive via `/opsx:archive`.

> **As-built (2026-09-18):** `foldName` (NFD + `\p{M}` strip + 8-letter fold table + punctuation collapse), `resolvePlayer` with exact-web → exact-full → prefix → substring precedence and `totalPoints` tie-break, `suggestPlayers` (whole query, per-token, and 4-letter stem fallback so a typo like "Odegard" still suggests Ødegaard). All five name-taking tool paths return `{ notFound, query, suggestions }` (transfer adds `side`); `fmtPlayer` tags `owned` from a per-context `ownedById` map; `search_players` gains `excludeOwned` (default true) and a "TRANSFER TARGETS" description; `GROUNDING_RULES` (not-found ⇒ spelling only; owned ⇒ never a transfer target, lineup call, price never counted) injected into both manager and demo prompts. Tests: 10 new cases (`name-resolution.test.ts` + `tools.test.ts` updates); suite 383/383 (53 files); `tsc` clean; `eslint` 0 errors; both replays byte-identical. **Live verification (tool layer, real squad 2558300 + 659-player universe):** `odegaard`/`martin odegaard` → Ødegaard (xi); `gross`/`joao pedro`/`sangare` → Groß (bench), João Pedro (xi), M.Sangaré (xi); `Odegard` → `notFound`, suggestions `["Ødegaard"]`; `search_players FWD ≤ £7.7m` → 8 rows, all `owned: null`, no João Pedro/Wissa; with `excludeOwned: false` Wissa appears tagged `xi`; `get_squad` tags all 15. **Model prose check (live `/api/ask`, manager 2558300):** first attempt at 19:59Z failed on every call — Anthropic credit exhausted ("Your credit balance is too low") — recorded as `unavailable` at the time; Kavit restored credit and the check was re-run at ~20:10Z with 0 credit errors. (1) "score odegaard" → `score_player` resolved Ødegaard, scored him as an XI hold. (2) "score Odegard" → resolved cleanly, but because the MODEL corrected the spelling before calling the tool, so the not-found path was forced with names the model cannot correct: "score Oscar Bobb-Petersen" → tool `notFound` with suggestions → answer: *"The name didn't match in the data — the suggestions include Bobb … Want me to score Bobb instead?"*; "how is Odegrad doing" → tool `notFound` (suggestion Ødegaard) → *"The name didn't quite match — did you mean Ødegaard?"* then re-queried and scored him, noting "You already own him in your XI". No injury/absence invented in any answer. (3) "which forward under £7.7m should replace Wissa?" → `get_squad` + `search_players` → Emersonn £5.5m recommended, alternatives Calvert-Lewin and Havertz, explicitly "not in your squad"; João Pedro not mentioned. Earlier note on the live eval stands: captures made while credit was exhausted (none — the GW6 capture at 19:36Z preceded it) would have fail-safed the LLM context to neutral.
