# Tasks — docs as a product pitch

### Task 1: README — lead with the pitch
**Capability:** docs-product-pitch
- Rewrite the top of `README.md` to the story arc (pain → promise → the three differentiators → proof →
  try it), keeping the Pocket Scout voice and the architecture-at-a-glance mermaid. The reader is an FPL
  manager first, an engineer second.

### Task 2: README — final-state correctness
**Capability:** docs-product-pitch
- Replace stale transfer copy: 1-or-2 toggle → **0–5 free transfers**; single move → **up to N stacked
  moves**; composite restructure → **expected-points restructure chosen by the allocator** (otherwise an
  alternative priced against the remaining FT).
- Update the test count in "Tech stack" to the current number (331).

### Task 3: ARCHITECTURE.md — optimizer / allocation
**Capability:** docs-product-pitch
- Rewrite the optimizer paragraph to describe the **optimal free-transfer allocation** (swaps cost 1,
  restructures cost 2; maximize expected-points-net-of-banking within the FT budget; feasibility for
  bank / 3-per-club / distinct players) and the ep-native restructure. Clarify the FT range (0–5) on the
  cache key.

### Task 4: EVALUATION.md — gate + allocation note, test count
**Capability:** docs-product-pitch
- Keep the hold-gate narrative; add a line that the same expected-points bar now governs **multi-move
  allocation and restructures** (one consistent currency). Update any stale test count.

### Task 5: Image catalog + screenshot request
**Capability:** docs-product-pitch
- Update `docs/images/README.md` and the README image references for the new/updated shots
  (`fpl-advisor-hero.png` updated, `fpl-advisor-this-week.png` updated, `fpl-advisor-free-transfers.png`
  new) plus the existing `fpl-advisor-demo-mode.png` placeholder — each present or marked "to be supplied".
- Land the prose with placeholders; the user supplies the images (see the request list).

### Task 6: Verify
**Capability:** docs-product-pitch
- Docs-only — no code gate. Proof-read for internal consistency (no stale toggle/single-move/composite
  copy; links resolve; image references match the catalog). Confirm the README renders cleanly.
